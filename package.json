{
  "name": "secure-notes-app",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
     "test": "turbo run test",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.9.16"
  }
}